---
type Props = {
  skills: string[];
};

const { skills } = Astro.props;
---

<div class="flex flex-wrap gap-1.5 mt-4">
  {skills.map((skill) => (
    <span class="font-mono text-label text-secondary bg-edge-subtle border border-edge px-3 py-1 rounded-sm">
      {skill}
    </span>
  ))}
</div>
