---
interface Props {
  name: string;
  icon: string;
  url: string;
}

const { name, icon, url } = Astro.props;
---

<a aria-label={name} href={url} target="_blank" class="hover:text-blue-500 dark:hover:text-blue-500 transition-colors">
  <span class="text-2xl">{name}</span>
</a>
