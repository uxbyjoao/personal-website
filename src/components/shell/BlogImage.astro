---
import { Image } from "astro:assets";

interface Props {
  src: string | import("astro").ImageMetadata;
  alt: string;
  caption?: string;
  width: number;
  height: number;
}

const { src, caption, alt, width, height } = Astro.props;
---
<figure>
  <Image
    src={src}
    sizes="(max-width: 800px) 100vw, 800px"
    alt={alt}
    width={width}
    height={height}
  />
  {caption && <figcaption>{caption}</figcaption>}
</figure>
